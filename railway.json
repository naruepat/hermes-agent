{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "/init /opt/hermes/docker/main-wrapper.sh sh -c 'bash /opt/data/dataforseo_setup/ensure_mcp.sh || true; exec hermes gateway run'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
